/**
 * src/app/environments/environment.ts  (development)
 */
export const environment = {
  production: false,
  apiBaseUrl: '',       // empty = relative URL → proxied by Angular dev server
  wsBaseUrl: 'ws://localhost:8000',
};
